# Populated once the Terraform infra/modules/contact-form API Gateway exists
# (see infra/outputs.tf -> contact_form_api_endpoint). Empty locally = the
# contact form's submit handler no-ops instead of posting anywhere.
PUBLIC_CONTACT_FORM_ENDPOINT=
